SERVICE=plex
SERVICEPORT=32400
TS_AUTHKEY=replace-with-your-reusable-tailscale-auth-key
TZ=Etc/UTC

IMAGE_URL=lscr.io/linuxserver/plex:latest
PLEX_CLAIM=claim-xxxxxxxxxxxxxxxxxxxx
PLEX_CONFIG_PATH=./plex-data/config
PLEX_MEDIA_PATH=./plex-data/media
DNS_SERVER=1.1.1.1

# Base directory for this service's config/state volumes (Tailscale state,
# app config). Defaults to "." (this directory) if unset. Set this to move
# the whole service's persistent data elsewhere, e.g. an external drive.
# Does NOT affect PLEX_MEDIA_PATH — override that separately for your media library storage.
HOME_LAB_DIR=.
