SERVICE=dante
SERVICEPORT=1080
TZ=Etc/UTC

IMAGE_URL=debian:bookworm-slim

# Base directory for this service's persistent data (config).
# Defaults to "." (this directory) if unset. Set this to move the whole
# service's persistent data elsewhere, e.g. an external drive.
HOME_LAB_DIR=.
