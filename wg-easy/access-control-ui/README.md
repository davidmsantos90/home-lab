# wg-easy Access Control UI

React + UI Kit dashboard for RFC-007 access control.

## How to use

```sh
npm install
cp .env.example .env
npm run dev
```

Set `VITE_ACCESS_CONTROL_API_URL` if the access-control API is not running on
`http://127.0.0.1:8787`.
